<%= form_with(model: import_file) do |form| %>
  <% if import_file.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(import_file.errors.count, "error") %> prohibited this import_file from being saved:</h2>

      <ul>
        <% import_file.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :file, style: "display: block", for: "formFile", class: "form-label" %>
    <%= form.file_field :file, class:"form-control form-control", id:"formFile" %>
  </div>

  <div class="text-end mt-5">
    <%= form.submit "Salvar", class: "btn btn-success" %>
  </div>
<% end %>


